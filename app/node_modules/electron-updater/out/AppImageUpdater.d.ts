import { CancellationToken, AllPublishOptions, VersionInfo } from "builder-util-runtime";
import "source-map-support/register";
import { FileInfo, UpdateCheckResult } from "./main";
import { BaseUpdater } from "./BaseUpdater";
export declare class AppImageUpdater extends BaseUpdater {
    constructor(options?: AllPublishOptions | null, app?: any);
    checkForUpdatesAndNotify(): Promise<UpdateCheckResult | null>;
    /*** @private */
    protected doDownloadUpdate(versionInfo: VersionInfo, fileInfo: FileInfo, cancellationToken: CancellationToken): Promise<Array<string>>;
    protected doInstall(installerPath: string, isSilent: boolean, isForceRunAfter: boolean): boolean;
}
